Python web server 

Edit files inside public/ to change the website. Start the server from the
Homebru dashboard, then open http://localhost:{{PORT}} on this computer.
Other computers on your LAN can use the server computer's LAN IP. You may need
to allow Python through the operating-system firewall.
